import React from "react";

const Loading = () => {
  return (
    <div className="absolute w-screen h-screen bg-primary text-white text-5xl flex items-center just-center z-[999999]">
      Loading ...
    </div>
  );
};

export default Loading;
